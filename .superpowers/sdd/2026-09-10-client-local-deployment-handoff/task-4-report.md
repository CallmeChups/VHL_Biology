# Task 4 Validation Report — Client Local Deployment Handoff

## Release under review

- Release version: `1.0.0-client`
- Release output: `.\release-test`
- Manifest source commit: `4882ae0518b78ef6b2fde539c8394f8dcbd2bad3`
- Declared runtime: Python 3.11 (`runtime.txt`)
- Runtime available during validation: Python 3.12.10
- Conda: unavailable on `PATH`
- Direct fallback service packages: FastAPI 0.115.0, Uvicorn 0.30.6,
  Requests 2.32.5. The pinned release dependency versions remain recorded in
  `RELEASE_MANIFEST.json`.

## Validation results

### Existing tests

Command:

```powershell
pytest backend/tests tests -q
```

Result: **PASS — 44 passed**. Pytest emitted two pre-existing cache permission
warnings for `.pytest_cache`; no test failed.

### Release build

Command:

```powershell
Remove-Item -Recurse -Force .\release-test -ErrorAction SilentlyContinue
.\scripts\build_release.ps1 -Version 1.0.0-client -OutputDirectory .\release-test
```

Result: **PASS**. The builder produced the versioned package and manifest. The
package contained 49 allowlisted runtime, model, sample, training-artifact,
script, and client-document files.

### Exclusion and portability audit

The required audit passed after removing Python bytecode caches generated by
local execution of the package:

- No `temp.txt`, `temp.xlsx`, `.env`, `.venv`, pytest cache, frontend log, or
  Python bytecode files remained in the release output.
- No `D:\`, `C:\Users`, `/home/`, or `/workspace/` path was present in the
  release output.
- Documentation placeholder/path scan found no `TODO`, `TBD`, or developer
  path.

The audit initially exposed a hard-coded `D:\incoming\...` example in the
operations runbook. This directly-coupled portability defect was fixed by
deriving replacement paths from the current workspace. No model or inference
behavior was changed.

### Local service and pipeline checks

Because Conda is unavailable, the supplied Conda launchers could not start:
both `start_backend.ps1` and `start_dashboard.ps1` correctly reported
“Conda was not found on PATH” and returned exit code 1.

As an available fallback, the packaged backend and dashboard were started
directly with the available Python 3.12.10 environment from `release-test`.

`health_check.ps1` result:

```text
Backend http://127.0.0.1:8000/health -> HTTP 200
Dashboard http://127.0.0.1:8501 -> HTTP 200
```

The packaged smoke test completed every pipeline stage:

```text
PASS session
PASS upload (9415 signal points)
PASS peaks (20 rows)
PASS classification (gga)
PASS phase (20 rows)
PASS toxicity (10.05%)
```

The smoke command then returned exit code 1 because approved-baseline
verification failed. This is an acceptance failure, not a fabricated pass:

- Expected: 20 peaks, `GGA`, probability `0.784`, toxicity `5.31`
- Observed: 20 peaks, `gga`, probability `0.7018118473814843`, toxicity
  `10.05`
- Mismatches: `classification_probability`, `toxicity_percent`
- Runtime verification: unverified (`3.12.10` versus declared `3.11`)

The final `RELEASE_MANIFEST.json` records the observed values and
`acceptance_status: "failed"`. Model checksum verification passed for all 12
manifest model entries.

### LAN validation

LAN acceptance was **not available** in this environment. No second LAN
machine, client browser, or firewall-rule validation was available. Therefore
access through `http://<server-ip>:8501` and confirmation that ordinary users
do not require port 8000 remain unverified. Local dashboard reachability on
port 8501 was verified. No LAN screenshot could be captured; the command
outputs above are the available acceptance evidence.

## Self-review

- Runtime architecture and port/interface documentation: present and
  internally consistent.
- Allowlisted release contents and exclusions: verified.
- Windows/Conda setup and operational procedures: documented.
- Model card, API reference, checksums, representative sample, and smoke test:
  present and checked.
- Docker/public deployment: explicitly out of scope.
- No independent model retraining or model behavior changes were introduced.
- Existing untracked `frontend_logs*` files were left untouched.

## Acceptance conclusion

The release assembly, package exclusions, local health checks, pipeline stage
execution, manifest checksums, and documentation portability checks are
verified. Clean Python 3.11/Conda installation and second-machine LAN
acceptance are unavailable. The representative output does not match the
approved baseline, so client acceptance must remain **FAILED** pending review.
